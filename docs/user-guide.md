# Руководство пользователя

## Загрузка файлов

### Изображения

- Поддерживаются только изображения (`image/*`: JPEG, PNG, GIF, WebP и т.п.).
- При загрузке каждое изображение:
  - при необходимости уменьшается по ширине до 1920px;
  - перекодируется в WebP с качеством ~75;
  - исходный файл (в оригинальном формате) **не сохраняется**.

Файлы хранятся раздельно для каждого пользователя:
- изображения — по пути `/uploads/{userId}/images/YYYY/MM/filename.webp`.

### Прочие вложения (PDF, DOCX и т.п.)

- Для «обычных» файлов (PDF, DOCX, XLSX и т.п.) есть два варианта:
  1. **Через Яндекс.Диск** — рекомендованный способ:
     - Включить интеграцию с Яндекс.Диском (см. ниже).
     - Прикреплять файлы к блокам через кнопку «скрепка» — в этом случае:
       - сам файл загружается в вашу app‑папку на Яндекс.Диске;
       - в Memus хранится только логический путь вида `app:/...` и сервисная ссылка.
  2. **Через локальные вложения** (на сервер Memus):
     - В некоторых случаях (например, импорт, устаревшие заметки) вложения могут храниться в `uploads/{userId}/attachments/{articleId}/...`.
     - Для новых заметок этот способ используется ограниченно и в основном для совместимости и импорта.

> Доступ к любым загрузкам жёстко привязан к пользователю: даже по прямой ссылке нельзя открыть файлы другого пользователя.

Доступ к загрузкам жёстко привязан к пользователю: даже по прямой ссылке нельзя открыть файлы другого пользователя.

### Вложения с Яндекс.Диска

- Поддерживается интеграция с Яндекс.Диском:
  - Вход через Яндекс‑аккаунт.
  - Загрузка вложений (PDF, DOCX и т.п.) напрямую в app‑папку на диске пользователя.
  - Открытие файлов через `/api/yandex/disk/file?path=...`:
    - при первом открытии файл автоматически «публикуется» на Яндекс.Диске;
    - затем используется его стабильная `public_url` или, при необходимости, прямая ссылка на скачивание.
- В Memus сохраняются только:
  - логический путь (`app:/...` или `disk:/...`);
  - имя файла и служебные метаданные (тип, размер).

## Интеграция с Telegram

### Привязка аккаунта Telegram к Memus

В меню пользователя (иконка пользователя внизу сайдбара) есть пункт **«Привязать Telegram»**.

Поток:
1. Нажать «Привязать Telegram».
2. Приложение запросит на сервере одноразовый токен и покажет готовую команду вида:
   - `/link UldUeO35K_ZyADmrb0ZaJWQg`
3. Открыть чат с ботом **@Memus_pro_bot** в Telegram.
4. Отправить боту эту команду `/link ...`.
5. Бот свяжет ваш Telegram‑чат с текущим аккаунтом Memus:
   - каждое сообщение из этого чата будет превращаться в быструю заметку в вашем Inbox.

Ограничения:
- Привязка действует **на один чат** (личный диалог, группа и т.п.).
- Токен `/link ...` одноразовый и действует ограниченное время; если команда не сработала — сгенерируйте новый токен в Memus.

### Что делает Telegram‑бот

После успешной привязки:

- Любое сообщение в привязанном чате:
  - текст попадает в новый блок в разделе «Быстрые заметки» вашего Inbox;
  - фото:
    - скачивается ботом из Telegram;
    - сохраняется как обычное изображение Memus в `/uploads/{userId}/images/...`;
    - вставляется в блок `<img>` так же, как если бы вы загрузили её через интерфейс;
  - документы/аудио/видео/голосовые:
    - загружаются в вашу app‑папку на Яндекс.Диске;
    - регистрируются как вложения статьи;
    - в тексте блока появляется ссылка с **исходным именем файла** (например, `загранпаспорт.pdf`, а не `file_3.pdf`).

Без привязки:
- Бот не создаёт заметки и подсказывает, что нужно:
  - в Memus сгенерировать код привязки;
  - отправить команду `/link <код>` в чат с ботом.

### Пункты меню Telegram в интерфейсе

В меню пользователя доступны пункты:
- **Привязать Telegram** — сгенерировать одноразовый код `/link ...` для текущего пользователя.
- **Телеграм‑бот** — открыть чат с @Memus_pro_bot.
- **Обратная связь** — открыть чат с @Memus_feedback_bot.

## Экспорт и импорт статей

В меню списка статей (`…` над списком) доступны пункты:
- **«Резервная копия (ZIP)**» — скачать ZIP со всеми вашими страницами в формате HTML.
- **«Импорт из HTML»** — создать новую страницу из такого HTML‑файла.
- **«Импорт из Markdown»** — создать новую страницу из `.md`‑файла с упрощённым списковым синтаксисом.
- **«Импорт из Logseq»** — импортировать сразу несколько страниц из архива Logseq.

В меню текущей статьи (`…` в шапке статьи) доступны пункты:
- **«Экспорт в HTML»** — сохранить текущую страницу в один HTML‑файл.

### Экспорт в HTML

Что делает система:
- Строит отдельную страницу с разметкой и стилями (почти как в Memus).
- Вставляет в `<head>` скрытый JSON‑снапшот статьи:
  - блок `<script type="application/json" id="memus-export">` содержит `version`, `article` и `blocks` в том же формате, что API.
- Изображения и вложения, встраиваемые в текст, по возможности конвертируются в `data:`‑URL (WebP / base64), чтобы файл был самодостаточным.
  - при этом внутри HTML дополнительно сохраняется «исходный» путь до файла (`data-original-src="/uploads/..."`, `data-original-href="/uploads/..."`), чтобы при обратном импорте можно было переиспользовать уже существующие файлы и не плодить дубликаты в `uploads`.

Что делает пользователь:
- Открыть статью, выбрать в меню `… → Экспорт в HTML`.
- Сохранить файл `*.html` на диск.
- Позже можно:
  - открыть его в браузере как статичную «печать» заметки;
  - загрузить его обратно через «Импорт из HTML».

Ограничения:
- Импорт поддерживает **только** файлы, созданные через текущую версию «Экспорт в HTML» в Memus.

### Резервная копия всех страниц (ZIP)

Что делает система:
- по кнопке `… → Резервная копия (ZIP)` в меню списка статей формирует ZIP‑архив;
- внутрь архива кладутся **все ваши статьи** (кроме удалённых) как отдельные HTML‑файлы;
- каждый файл:
  - содержит полное дерево блоков и стили, похожие на основной интерфейс;
  - включает блок `<script type="application/json" id="memus-export">` с JSON‑снапшотом статьи;
  - **самодостаточен**: все изображения и вложения, на которые ссылается статья, инлайнены как `data:`‑URL;
  - при этом для каждого инлайненного файла сохраняется исходный путь `data-original-src="/uploads/..."` / `data-original-href="/uploads/..."`, чтобы при восстановлении на том же сервере можно было переиспользовать существующие файлы.

Как использовать:
- периодически скачивайте ZIP‑архив и храните его в личном хранилище;
- при необходимости можно:
  - открыть любой HTML из архива в браузере как статичную «распечатку» страницы;
  - выбрать отдельный файл и импортировать его обратно в Memus.

Особенности:
- зашифрованные статьи сохраняются **в том виде, как они хранятся на сервере** (контент блоков остаётся зашифрованным);
- ZIP‑архив — пользовательский бэкап страниц, он **не заменяет** резервное копирование базы данных на уровне сервера.

### Импорт из резервной копии (папка)

В меню списка статей (`…` над списком) есть пункт:
- **«Импорт из резервной копии (папка)»** — массово импортирует все HTML‑файлы из выбранной папки (например, распакованный ZIP‑бэкап).

Что делает система:
- Открывается диалог выбора папки; браузер передаёт все файлы внутри.
- Для каждого `*.html`:
  - читается блок `memus-export` и берётся `article.id` (UUID) и заголовок;
  - выполняется «умный» импорт:
    - если статьи с таким UUID ещё нет — создаётся новая страница;
    - если статья уже существует — показывается диалог варианта восстановления (см. ниже).
- Процесс идёт последовательно, внизу показывается прогресс и итоговое число импортированных страниц.

Поведение при конфликте (страница с таким UUID уже есть):
- Появляется диалог «Страница уже существует» с информацией:
  - название в базе / в файле;
  - даты `createdAt` / `updatedAt` в базе и в файле.
- Доступны варианты:
  - **«Оставить существующую»** — страница из резервной копии для этого UUID **не импортируется**;
  - **«Перезаписать»** — содержимое существующей страницы полностью заменяется версией из HTML‑файла, при этом UUID сохраняется;
  - **«Создать копию»** — создаётся новая страница с новым UUID и заголовком вида  
    `ver_YYYYMMDD_HHMMSS <исходное название>` (время берётся из даты/времени изменения файла).
- Дополнительно можно включить флажок:
  - **«Применять это решение ко всем конфликтам»** — выбранное действие будет автоматически применяться ко всем последующим конфликтующим страницам в рамках текущего импорта.

Импорт одиночного HTML (`… → Импорт из HTML`) использует ту же модель:
- если UUID в файле не найден в базе — создаётся новая страница;
- если есть конфликт по UUID — показывается тот же диалог выбора варианта (но без флажка «ко всем», так как файл один).

### Импорт из HTML

Что делает система:
- Загружает HTML‑файл на сервер.
- Находит внутри `<script id="memus-export">` и разбирает JSON‑снапшот:
  - структура блоков и текста берётся из `blocks`,
  - метаданные статьи — из `article`.
- В теле HTML ищет:
  - `img[src^="data:"]` — извлекает байты, сохраняет как обычные изображения в `uploads/{userId}/images/...`, переписывает `src` на `/uploads/...`;
  - ссылки‑вложения (`a.attachment-link[href^="data:"]`) — сохраняет в `uploads/{userId}/attachments/{articleId}/...`, переписывает `href`.
- Создаёт **новую** статью в базе, не перезаписывая существующие.
- Импортированная статья обычная (не зашифрована); при необходимости её можно отдельно «Зашифровать».

Что делает пользователь:
- В меню статьи выбрать `… → Импорт из HTML`.
- Выбрать ранее экспортированный HTML‑файл.
- Дождаться окончания импорта; при успехе откроется новая страница‑копия.

### Импорт из Markdown (.md)

Поддерживается упрощённый формат «деревянного» списка:

- Структура:
  - каждый блок начинается с новой строки и символа `-`;
  - уровень вложенности определяется количеством табов (`\t`) **перед** `-`:
    - без табов — корневой блок;
    - один таб — ребёнок предыдущего блока;
    - два таба — внук и т.д.
  - строки, начинающиеся (после табов) с `collapsed::` или `logseq.` — игнорируются и не попадают в текст блоков.
- Текст:
  - внутри строки `**жирный текст**` становится `<strong>жирный текст</strong>`;
  - остальное воспринимается как обычный текст (без полноценного Markdown);
  - пустая строка внутри блока превращается в пустой абзац.
- Заголовки блоков:
  - если первая строка блока начинается с `#`, `##`, `###` или `####`,
    то её текст (без `#`) считается заголовком блока;
  - после такой строки автоматически вставляется пустая строка‑разделитель,
    чтобы заголовок корректно отделился от тела блока.
- Ссылки‑вложения:
  - конструкции `![alt](url)`:
    - если `url` начинается с `http://` или `https://`:
      - создаётся обычная ссылка `<a href="url" target="_blank">alt или url</a>`;
    - если это относительный путь (`../assets/file.pdf`, `./file.zip` и т.п.):
      - создаётся `<a class="attachment-link" href="url">имя файла</a>`;
      - если при импорте указан базовый URL для assets (например, Netlify),
        приложение попытается:
        - извлечь относительный путь **после** `assets/` (например, из `../assets/1.pdf` взять `1.pdf`);
        - скачать файл по адресу `<base>/<относительный_путь>` **или** `<base>/assets/<относительный_путь>`;
        - сохранить его в `uploads/{userId}/attachments/{articleId}/...` и переписать ссылку на внутренний `/uploads/...` URL.

Заголовок импортированной статьи:
- Всегда берётся имя файла без расширения и сохраняется без изменений (без эвристик).
- Чтобы ссылки вида `[[Страница]]` корректно работали, имеет смысл сохранять `.md` под тем же именем, что и заголовок будущей страницы в Memus.

Что делает пользователь:
- В меню статьи выбрать `… → Импорт из Markdown`.
- Выбрать `.md` (или `.txt`) в описанном формате.
- При необходимости скорректировать структуру блоков уже в редакторе.

Пример:

- `- #Континуум дисторсии` — корневой блок с заголовком «Континуум дисторсии».
- `\t- Либо она **выпуклая**` — дочерний блок 1‑го уровня с жирным фрагментом.
- пустой `-` на отдельной строке создаёт пустой блок.

### Импорт из Logseq

Этот импорт предназначен для переноса графа Logseq (страницы + вложения).

Формат архива:
- Ожидается ZIP:
  - папка `pages/` — внутри Markdown‑файлы страниц (`*.md`, `.markdown` или `.txt`);
  - папка `assets/` — вложения (изображения, PDF, прочие файлы), на которые идут ссылки из Markdown.
- `assets/` внутри архива **не обязателен**, если вложения лежат на внешнем хостинге (см. ниже).

Требования к кодировке ZIP:
- Имена файлов и папок в ZIP должны быть в **UTF‑8**.
- Если в архиве есть русские (или другие не‑ASCII) имена и ZIP создан в старой Windows‑кодировке (без флага UTF‑8), импорт будет отклонён с ошибкой.
- На Windows для правильного архива удобно использовать бесплатный архиватор **PeaZip** (<https://peazip.github.io/>) и включить у него сохранение имён файлов в UTF‑8.

Поведение при импорте:
- Каждая страница `pages/<имя>.md` превращается в **отдельную статью**:
  - заголовок статьи всегда равен имени файла без расширения (ровно как в Logseq), без попыток «чинить» странную кодировку;
  - поэтому важно, чтобы ZIP был создан с корректными UTF‑8‑именами файлов.
  - содержимое `.md` разбирается теми же правилами, что и «Импорт из Markdown»:
    - вложенность по табам,
    - игнорирование `collapsed::` и `logseq.*`,
    - `**жирный**`,
    - заголовки по `#`.
- Вложения:
  - конструкции `![](../assets/...)` или `![](assets/...)` считаются ссылками на файлы в `assets/`;
  - для каждой такой ссылки Memus:
    1. Сначала ищет файл внутри архива:
       - если находит — читает байты и сохраняет их в `uploads/{userId}/attachments/{articleId}/filename.ext`;
       - ссылка в тексте переписывается на внутренний `/uploads/...` URL.
    2. Если в архиве файла нет, но указан внешний базовый URL (см. ниже) —
       извлекает относительный путь после `assets/` и пытается скачать:
       - `<базовый URL>/<относительный_путь>` (например,  
         `https://example-site.netlify.app/1_файл.pdf`);
       - если не получилось — `<базовый URL>/assets/<относительный_путь>`;
       после успешной загрузки файл сохраняется как вложение статьи и ссылка переписывается на `/uploads/...`.
    3. Если ничего не удалось найти/скачать — ссылка остаётся как есть.
  - Внешние `![](https://...)` остаются внешними ссылками и не загружаются на сервер.

Использование внешнего хранилища для `assets` (например, Netlify):
- Часто удобнее не тянуть тяжёлые вложения в ZIP, а выложить их на статику.
- Поддерживается схема:
  1. Ты выкладываешь `assets/` на любой публичный хостинг (например, Netlify) так,
     чтобы файлы были доступны по URL вида  
     `https://example-site.netlify.app/имя_файла` **или**  
     `https://example-site.netlify.app/assets/имя_файла`.
  2. В Memus при выборе «Импорт из Logseq» сначала показывается диалог
     с просьбой **указать базовый URL для assets**, например:  
     `https://prismatic-salamander-2afe94.netlify.app`.
     - Это значение сохраняется в браузере (localStorage), чтобы не вводить каждый раз.
  3. Затем ты выбираешь ZIP с папкой `pages/` (можно без `assets/`).
  4. При разборе ссылок `assets/...` приложение:
     - если файл есть в ZIP — берёт оттуда;
     - иначе — пробует:
       - `<базовый URL>/<относительный_путь>` (например,  
         `https://prismatic-salamander-2afe94.netlify.app/1_раздел_Урок_3_....pdf`);
       - если не получилось — `<базовый URL>/assets/<относительный_путь>`;
     - после успешной загрузки файл кладётся в `uploads/{userId}/attachments/{articleId}/...`,
       а ссылка в тексте переписывается на `/uploads/...`.

Что делает пользователь при импорте Logseq:
1. Подготовить данные:
   - экспортировать / скопировать из Logseq папку `pages/` (страницы);
   - либо положить `assets/` в тот же ZIP, либо заранее выложить `assets/` на Netlify/другой хост.
2. В Memus:
   - Нажать `… → Импорт из Logseq`.
   - Ввести базовый URL, где лежат `assets` (если используешь внешний хост).
   - Выбрать ZIP с `pages/` (и, при желании, `assets/`).
3. Дождаться завершения импорта:
   - при успехе откроется первая импортированная страница;
   - в тосте отображается, сколько страниц было импортировано.

Ограничения:
- Максимальный размер ZIP‑файла для импорта — около 1 ГБ; очень большие архивы могут не загрузиться.
- После завершения импорта временные файлы и сам архив удаляются с сервера.

## Ссылки на другие страницы (`[[...]]`)

В тексте блоков поддерживаются «wikilinks» в логике Logseq/Obsidian:

- Синтаксис:
  - `[[Название страницы]]` — ссылка на другую статью в Memus.
- Как это работает:
  - при сохранении блока сервер ищет среди статей текущего пользователя
    первую статью, у которой заголовок (`title`) совпадает с `Название страницы`
    **без учёта регистра**;
  - если такая статья найдена, `[[Название страницы]]` заменяется в HTML на
    обычную ссылку  
    `<a href="/article/{id}">Название страницы</a>`;
  - если статья не найдена (или удалена), текст `[[...]]` остаётся как есть,
    без создания ссылки.
- Где применяется автоматическое разворачивание wikilinks:
  - при импорте из Markdown;
  - при импорте из Logseq;
  - при обычном сохранении блока в редакторе (в т.ч. после вставки из буфера).
- Особенности:
  - новые статьи по `[[...]]` **не создаются автоматически** — сначала нужно
    завести страницу с таким заголовком;
  - повторное сохранение блока не «дублирует» ссылки: уже заменённый
    `<a href="...">...</a>` не трогается, обрабатываются только сырые `[[...]]`.

## Учётные записи и доступ

- Перед работой с приложением нужно войти через Google‑аккаунт.
- Регистрация и вход по логину/паролю сейчас отключены (код сохранён только на сервере).
- Каждый пользователь видит **только свои** статьи, блоки, результаты поиска и файлы — общих страниц нет.
- Статью может редактировать только её автор; другие пользователи не видят и не могут менять чужие статьи.
- В правой нижней части сайдбара отображается текущий пользователь и кнопка выхода.

### Вход через Google

- На стартовом экране есть единственная кнопка:
  - «**Войти через Google**» — открывает стандартное окно авторизации Google.
- После успешного входа:
  - в Memus создаётся или используется привязанный к вашему Google‑аккаунту пользователь;
  - в правом нижнем углу сайдбара отображается имя/логин Google‑пользователя.
- Выйти из аккаунта можно кнопкой «**Выйти**» внизу сайдбара.

> Администраторский аккаунт `kirill` теперь доступен **только** через Google‑логин `kirillnad@gmail.com`; вход по паролю под этим именем отключён.

## Шифрование статей

- Любую обычную статью (кроме Inbox) можно **зашифровать паролем** через меню статьи (`…` → «Зашифровать»).
- При первом включении шифрования:
  - приложение попросит ввести пароль;
  - можно добавить **подсказку** — короткое текстовое напоминание о пароле;
  - все блоки статьи будут зашифрованы и сохранены в таком виде в базе.
- При открытии зашифрованной статьи:
  - показывается диалог с запросом пароля;
  - если задана подсказка, она отображается под текстом «Введите пароль для этой страницы»;
  - после успешного ввода пароль **запоминается в текущей сессии браузера** и не запрашивается повторно, пока страница приложения открыта.
- Смена пароля:
  - в меню статьи выберите «Сменить пароль»;
  - введите новый пароль и (при необходимости) новую подсказку;
  - содержимое статьи будет **перешифровано** новым ключом.
- Снятие защиты:
  - в меню статьи доступна отдельная команда «Снять защиту»;
  - после подтверждения вся статья сохраняется в открытом виде, шифрование и подсказка для неё отключаются.
- Визуальные метки:
  - зашифрованные статьи помечаются **иконкой замка** перед заголовком;
  - цвет заголовка таких статей отличается от обычных, чтобы их было проще заметить.

## Публичные страницы

- Любую статью можно сделать публичной через меню `… → Сделать публичной`.
- При включении публичного доступа:
  - для статьи создаётся стабильный публичный адрес вида `/p/<случайный_код>`;
  - в заголовке страницы слева от названия появляется кнопка «🌐»:
    - по клику открывает публичную страницу в новой вкладке;
    - одновременно копирует её URL в буфер обмена.
- В списках статей:
  - в левом сайдбаре;
  - и в разделе «Статьи»

  — перед названием каждой **публичной** страницы показывается иконка `🌐`.

- Внутренние ссылки в публичной версии:
  - если ссылка ведёт на другую опубликованную страницу, она открывается как `/p/<slug>`;
  - если целевая страница ещё не опубликована, при клике показывается сообщение «Эта страница пока не опубликована».

Публичная страница «только для чтения»:
- редактирование блоков, перетаскивание и горячие клавиши отключены;
- при этом вся структура блоков, сворачивание/разворачивание и навигация по стрелкам работают так же, как в основном интерфейсе.

Состояние разворота дерева статей:
- Приложение запоминает отдельно, какие узлы свернуты в левом сайдбаре, и какие — в панели «Список статей».
- Оба состояния сохраняются между сессиями браузера и не зависят друг от друга.

## Управление пользователями

- Раздел «Пользователи» доступен только суперпользователю.
- Дополнительно к проверке прав на сервере:
  - при нажатии кнопки «Пользователи» запрашивается **пароль администратора** во всплывающем окне;
  - пароль передаётся только на сервер и сравнивается там, в клиентском коде он не хранится.
- В списке можно:
  - просмотреть всех пользователей;
  - удалить обычных пользователей (суперпользователя удалить нельзя).

## Горячие клавиши

### Режим просмотра (view)
- `Enter` — начать редактирование выбранного блока.
- `Ctrl+Z` / `Ctrl+Y` — откат / повтор.
- `Ctrl+Shift+ArrowDown` / `Ctrl+Shift+ArrowUp` — переместить блок вниз/вверх.
- `Ctrl+ArrowDown` / `Ctrl+ArrowUp` — создать блок после/до текущего.
- `Ctrl+Delete` — удалить блок.
- `Ctrl+ArrowRight` / `Ctrl+ArrowLeft` — вложить / вынуть блок из вложенности.
- `ArrowDown` / `ArrowUp` — выбрать следующий/предыдущий видимый блок.
- `ArrowRight` — раскрыть выбранный или перейти к дочернему.
- `ArrowLeft` — свернуть выбранный или перейти к родителю.

### Режим редактирования (edit)
- `Ctrl+Enter` — сохранить и выйти из редактирования.
- `Ctrl+ArrowRight` / `Ctrl+ArrowLeft` — вложить / вынуть текущий блок, оставаясь в редактировании.
- `Ctrl+ArrowDown` — разрезать блок по курсору (вторая часть станет новым блоком).
- `Ctrl+ArrowUp` — сохранить и создать блок выше.
- В панели под заголовком статьи есть кнопка «|↵ Разделить», которая делает то же самое, что `Ctrl+ArrowDown` (разделяет текущий блок по курсору).
- `Esc` — отменить редактирование.
- Пустой блок при выходе из редактирования (Esc, Cancel) или при сохранении удаляется автоматически.
- В режиме редактирования слева на месте кнопки сворачивания появляется кнопка `✕` (отмена изменений), а справа на месте кнопки добавления — `✔` (сохранение блока).

### Модальные окна/формы
- `Enter` — подтвердить.
- `Esc` — закрыть.

## Перетаскивание блоков (Drag & Drop)
- Включи режим перетаскивания кнопкой «⇅ Перетаскивание» в шапке статьи. Включённый режим действует только в режиме просмотра и только для обычных статей (не inbox).
- Включённый режим: тяни за любой участок блока (его поверхность), кроме интерактивных элементов (кнопки, поля ввода, ссылки и т.п.).
- Выключенный режим: блоки мышью не перетаскиваются, работают только горячие клавиши перемещения.
- Зона дропа: верх блока — вставить перед ним, низ — после, середина — вложить внутрь как последнего потомка.
- Запрещено переносить блок в самого себя или своих потомков; такие цели не подсвечиваются.
- Undo/Redo работают и для DnD: `Ctrl+Z` / `Ctrl+Y` откатывают/повторяют последнее перемещение.

## Быстрое добавление блока
- У выбранного блока справа отображается круглая кнопка с иконкой `+` — она добавляет новый блок **на том же уровне сразу под текущим** и сразу переводит его в режим редактирования.

## Меню форматирования (rich-context)
- В режиме редактирования правый клик (или долгий тап на телефоне) внутри блока открывает всплывающее меню форматирования.
- В левом столбце «Буфер» находятся иконки:
  - `⧉` — копировать выделенный фрагмент в буфер приложения и в стандартный буфер обмена;
  - `✂` — вырезать выделенный фрагмент (с сохранением в оба буфера);
  - `▣` — вставить содержимое внутреннего буфера в текущую позицию курсора;
  - `⛶` — выделить весь текст текущего блока.
- В колонках форматирования и структуры находятся иконки:
  - `Ж`, `/`, подчёркнутая `Ч`, `✕` — полужирный, курсив, подчёркивание и очистка форматирования;
  - `•`, `1.`, `❝`, `</>` — маркированный/нумерованный список, цитата и кодовый блок.
- В колонке ссылок:
  - `🔗` — вставить/отредактировать ссылку;
  - `⊘` — убрать ссылку;
  - `§` — вставить ссылку на другую статью.
- В правой колонке:
  - `|↵` — разрезать блок по текущему курсору и перенести всё, что ниже курсора, в новый блок-сосед (эквивалент `Ctrl+ArrowDown`).
- Вставка через кнопку «▣» использует только внутренний буфер редактора — вставить можно именно тот текст, который был скопирован/вырезан в этом же редакторе.
