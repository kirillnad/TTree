- # Интерактивное руководство Memus (пройдите шаги по порядку)
	- Как работать с этим руководством
		- Это “обучение действием”: после каждого шага сделайте действие в интерфейсе Memus и вернитесь сюда.
		- Если что-то пошло не так — в конце каждого шага есть “Если не получилось”.
		- Совет: держите открытыми две вкладки: Memus и это руководство.
	- Что получится в конце
		- Дерево статей (папка-проект + дочерние страницы)
		- Уверенное владение блоками: создание/вложенность/перемещение/слияние/разделение
		- Поиск + быстрый фильтр статей + граф
		- Вложения, таблицы, экспорт/бэкап, публичность, шифрование, Telegram (опционально)

- # Зачем нужен Memus? (контекст)
	- Memus — это личная база знаний: место, где хранится всё, что вы узнали или о чём подумали.
	- Задача: быстро записывать → легко находить → удобно приводить в порядок.

- # Концепция (как думать о Memus)
	- Статья
		- Статья — это “контейнер темы”: всё по одной теме собирается на одной странице.
	- Блоки
		- Статья состоит из блоков: блок — законченная единица мысли.
		- Заголовок блока получается автоматически: “первая строка + пустая строка” (дальше — тело).
	- Ссылки
		- Связывайте статьи ссылками `[[...]]`, чтобы навигация работала через граф, поиск и переходы по теме.

- # Шаг 1. Вход и ориентация (1–2 минуты)
	- Задание
		- Войдите в Memus (кнопка “Войти через Яндекс”; если у вас включён Google — можно через него).
		- Найдите глазами: левый сайдбар, “Статьи”, “Быстрые заметки”, строку поиска.
		- Откройте “Сстатьи” (кнопка в сайдбаре).
	- Опционально: установка на телефон (PWA)
		- Memus можно просто открыть в браузере.
		- А можно установить как приложение (PWA): откройте `https://memus.pro` в “родном” браузере платформы и добавьте на главный экран.
			- Android: Chrome
			- iOS: Safari
			- Huawei: Huawei Browser
	- Проверка
		- Внизу сайдбара вы видите меню пользователя (иконка) и своё имя в нём.
		- Вы понимаете, где “Список статей” и где “Статья”.
	- Если не получилось
		- Если оверлей входа не исчезает — перезагрузите страницу и попробуйте снова.

- # Шаг 2. Быстрые заметки (Inbox): “поймал мысль → сохранил”
	- Задание
		- Нажмите “Быстрые заметки”.
		- Нажмите “Новая” и добавьте 3 быстрые заметки (по 1–2 строки), например:
			- Идея: …
			- Ссылка: …
			- Вопрос: …
	- Проверка
		- В Inbox появились новые блоки-заметки.
	- Если не получилось
		- Если приложение ещё “тяжело” грузится, может появиться модалка “Быстрая заметка (оффлайн‑буфер)”: заметка сохранится локально и появится в Inbox, когда вы откроете “Быстрые заметки”.
		- Убедитесь, что вы не в режиме редактирования: нажмите Esc, затем пробуйте “Новая” ещё раз.

- # Шаг 3. Создайте “проект” и 2–3 страницы для структуры
	- Задание
		- Нажмите “Создать” в сайдбаре и назовите статью: Проект: обучение Memus.
		- Создайте ещё 3 статьи: План, Материалы, Идеи.
		- Откройте “Проект: обучение Memus” и добавьте ⭐ в избранное.
	- Проверка
		- В списке статей есть 4 статьи, “Проект: обучение Memus” помечен ⭐.
	- Если не получилось
		- Заголовок статьи редактируется через клик/двойной клик по названию в шапке статьи.

- # Шаг 4. Блоки: основы (создать → редактировать → заголовок → свернуть)
	- Задание (в статье “Проект: обучение Memus”)
		- Создайте 5 блоков (кнопка “Новый” в тулбаре или Ctrl+↑/↓).
		- Войдите в редактирование одного блока (Enter), напишите:
			- Первая строка: Цели
			- Пустая строка
			- Затем 2–3 строки текста
		- Сохраните (Ctrl+Enter).
		- Сверните/разверните блок (кнопка слева или стрелки ←/→).
	- Проверка
		- Вы умеете: выбрать блок кликом, войти в редактирование, сохранить, свернуть ветку.
	- Если не получилось
		- Если блок не сворачивается: у блока должен быть заголовок/дети; попробуйте сделать “первая строка + пустая строка + текст”.

- # Шаг 5. Навигация и безопасные правки (Undo/Redo)
	- Задание
		- Стрелками ↑/↓ выделяйте разные блоки.
		- Сделайте 2 изменения (например, поменяйте порядок двух слов в блоке).
		- Нажмите “Отменить” (или Ctrl+Z), затем “Повторить” (или Ctrl+Y).
	- Проверка
		- Изменения откатываются и возвращаются.
	- Если не получилось
		- Если вы в режиме редактирования блока — сначала сохраните Ctrl+Enter, затем делайте Ctrl+Z в режиме просмотра.

- # Шаг 6. Дерево статей (DnD + уровни)
	- Цель
		- Научиться делать структуру статей (родитель/дочерние) перетаскиванием и понимать 3 зоны дропа: до/после/внутрь.
	- Где тренируемся
		- Можно в левом сайдбаре (список статей) или в “Список статей” (центральный экран).
		- В режиме “Корзина” DnD не нужен (там удалённые статьи).
	- Подготовка (2 минуты)
		- Создай 6 статей: Проект, Идеи, План, Материалы, Черновики, Архив.
		- Открой “Все статьи”, чтобы видеть их списком.
	- Как работает DnD статей (важно)
		- Вставка ДО: отпускаешь на верхней части строки цели → статья окажется перед целью.
		- Вставка ПОСЛЕ: отпускаешь на нижней части строки цели → статья окажется после цели.
		- ВЛОЖИТЬ ВНУТРЬ: отпускаешь в середине строки цели → статья станет дочерней (подпунктом) цели.
	- Задание 1: Собери дерево
		- Перетащи Идеи ВНУТРЬ Проект.
		- Перетащи План ВНУТРЬ Проект.
		- Перетащи Материалы ВНУТРЬ Проект.
		- Перетащи Черновики ВНУТРЬ Проект.
		- Перетащи Архив ПОСЛЕ Проект (не внутрь).
	- Проверка 1 (должно получиться)
		- У Проект появились дочерние статьи: Идеи, План, Материалы, Черновики.
		- Архив остался на верхнем уровне и стоит после Проект.
	- Задание 2: Наведи порядок
		- Поменяй местами дочерние статьи внутри Проект перетаскиванием (например: План первым).
		- Сверни/разверни ветку Проект (в списке статей).
	- Проверка 2
		- Порядок детей изменился и сохраняется после перезагрузки/переходов.
	- Мобильная версия (если делаешь на телефоне)
		- Долгое нажатие по строке статьи (long press) → потяни → отпусти.
		- Не жми по “звёздочке” и другим интерактивным зонам строки — тащить нужно за саму строку.
	- Если “не тащится” или получается “не туда”
		- Попробуй другой участок строки (верх/середина/низ — это реально разные режимы).
		- Убедись, что ты не в “Корзине”.
		- Используй альтернативы:
			- Ctrl+Shift+↑/↓ — поднять/опустить статью
			- Ctrl+→/← — вложить/вынуть статью (изменить уровень)

- # Шаг 7. Перетаскивание блоков (Drag Mode)
	- Цель
		- Научиться перемещать блоки мышью/тачем: до/после/внутрь, и откатывать ошибки Undo.
	- Важно (условия)
		- Открой ОБЫЧНУЮ статью (не Inbox).
		- Переключись в режим просмотра (не редактирование текста блока).
		- Включи в тулбаре статьи кнопку “Двигать” (Drag Mode).
	- Как работает DnD блоков
		- Тянуть нужно за “поверхность” блока, не за ссылки/кнопки/поле редактирования.
		- Вставка ДО: дроп на верх цели.
		- Вставка ПОСЛЕ: дроп на низ цели.
		- ВЛОЖИТЬ ВНУТРЬ: дроп в середину цели (станет дочерним последним).
		- Нельзя переместить блок в самого себя или своего потомка — такие места не принимаются.
	- Подготовка (песочница)
		- Создай в статье блоки с такой структурой (любыми способами: “Новый”, Ctrl+↑/↓, Enter):
			- A
				- A.1
				- A.2
			- B
			- C
	- Задание 1: Вложить
		- Перетащи C ВНУТРЬ A (в середину A).
	- Проверка 1
		- C стал дочерним у A (например, A.3).
	- Задание 2: Поменять порядок
		- Перетащи A.2 ДО A.1 (в верх A.1).
	- Проверка 2
		- Внутри A порядок теперь: A.2, потом A.1.
	- Задание 3: Переставить на верхнем уровне
		- Перетащи B ПОСЛЕ A (если он уже там — сделай наоборот: ДО A).
	- Проверка 3
		- Верхний порядок блоков изменился.
	- Задание 4: Откатить
		- Нажми Ctrl+Z 2–3 раза (или кнопкой “Отменить”).
		- Затем Ctrl+Y (или “Повторить”).
	- Проверка 4
		- DnD‑перемещения откатываются и возвращаются.
	- Если DnD блоков мешает (или ты на Inbox)
		- Выключи “Двигать” и используй хоткеи:
			- Ctrl+Shift+↑/↓ — переместить блок
			- Ctrl+→/← — вложить/вынуть блок
			- Ctrl+Delete — удалить блок

- # Шаг 8. Форматирование: контекстное меню внутри блока
	- Задание
		- Открой редактирование блока (Enter).
		- Выдели кусок текста → открой меню форматирования (ПКМ/долгий тап).
		- Примените по очереди: жирный, курсив, список, цитату, код-блок, затем “очистить форматирование”.
	- Проверка
		- Вы умеете форматировать текст, не ломая структуру блоков.
	- Если не получилось
		- Меню доступно в режиме редактирования, при выделенном тексте или курсоре внутри блока.

- # Шаг 9. Ссылки и [[...]] (внутренние ссылки на статьи)
	- Задание
		- Создайте статью “Термины”.
		- В статье “Проект: обучение Memus” в тексте блока напишите: [[Термины]] и сохраните блок.
		- Перейдите по появившейся ссылке.
		- Добавьте внешнюю ссылку на любой сайт (через контекстное меню “🔗” или просто вставкой URL).
	- Проверка
		- [[Термины]] превратилось в кликабельную ссылку на вашу статью.
	- Если не получилось
		- Важно: статьи по [[...]] не создаются автоматически — сначала создайте статью с таким заголовком.

- # Шаг 10. Поиск + быстрый фильтр статей (нижнее поле в сайдбаре)
	- Задание: поиск по контенту
		- Введите в строку поиска слева слово из ваших блоков.
		- Откройте результат типа “Блок” и убедитесь, что открылась нужная статья и выбран нужный блок.
	- Как работает поиск (коротко)
		- Ищет по заголовкам статей и по содержимому блоков.
		- Результаты показываются в выпадающем списке под полем.
		- Клик по результату переводит в нужную статью/блок.
	- Задание: быстрый фильтр по названиям статей
		- Внизу сайдбара в поле “Фильтр статей…” начните вводить часть названия вашей статьи.
		- Очистите фильтр кнопкой “×”.
		- Нажмите Esc в этом поле и убедитесь, что фильтр сбрасывается.
	- Проверка
		- Вы различаете:
			- Поиск (по тексту блоков и статьям)
			- Быстрый фильтр (только по списку статей в сайдбаре)
	- Если не получилось
		- Кликните в поле фильтра (в самом низу сайдбара), затем вводите.

- # Шаг 11. Граф страниц (видеть связи)
	- Задание
		- Нажмите кнопку графа в сайдбаре (иконка-граф).
		- Найдите на графе ваши страницы и откройте одну из них кликом.
	- Проверка
		- Вы понимаете: граф — это навигация по связям, а не “папки”.
	- Если не получилось
		- Если граф пустой: добавьте 1–2 [[...]] ссылки между страницами и откройте граф снова.

- # Шаг 12. Вложения: картинки и файлы (опционально)
	- Как вставлять изображения (быстро)
		- В режиме редактирования блока можно:
			- вставить картинку из буфера (Ctrl+V),
			- или перетащить файл мышью в блок.
	- Задание: прикрепить файл к блоку
		- Откройте редактирование блока (Enter).
		- Нажмите кнопку “Файл” внизу блока.
		- Выберите картинку или документ (если есть).
		- Сохраните блок и проверьте, что вложение отображается/открывается.
	- Задание: Яндекс.Диск (если включено на вашем сервере)
		- Попробуйте прикрепить PDF/DOCX: при необходимости файл загрузится на ваш Яндекс.Диск, а в Memus останется ссылка.
	- Про “обычные” файлы (PDF, DOCX и т.п.)
		- Рекомендуемый подход: хранить такие файлы в Яндекс.Диске/Google Drive и вставлять в Memus ссылку.
		- Если на вашем сервере включена интеграция/загрузка вложений — используйте кнопку “Файл”, но для больших архивов/документов всё равно лучше внешнее хранилище.
	- Проверка
		- Вы умеете добавлять материалы к знаниям, не смешивая всё в “один текст”.
	- Если не получилось
		- Кнопка “Файл” появляется только в режиме редактирования блока.

- # Шаг 13. Таблицы (быстрый структурированный конспект)
	- Задание
		- Откройте редактирование блока (Enter).
		- Нажмите “Таблица” в тулбаре статьи.
		- Заполните 2×2 (например: “Источник / Тезис”).
		- Кликните по ячейке и используйте панель таблицы: добавьте колонку и строку, затем удалите обратно.
		- Если таблица больше не нужна: “Таблица” → “Отменить таблицу (в текст)” (каждая ячейка станет отдельным абзацем).
	- Проверка
		- Вы умеете быстро фиксировать сравнения/списки в виде таблицы.
	- Если не получилось
		- Вставка таблицы доступна только в режиме редактирования блока.

- # Шаг 14. Экспорт, резервные копии и импорт (надёжность)
	- Экспорт одной статьи
		- В меню статьи (“…”) выберите “Экспорт в HTML”.
		- Откройте скачанный файл в браузере (это “статичная” версия).
	- Резервная копия всех страниц
		- В “Список статей” нажмите меню (“⋮”) → “Резервная копия (ZIP)”.
		- Скачайте архив.
	- Импорт (опционально, если хотите проверить круг)
		- Импорт из HTML: меню списка → “Импорт из HTML” (возможны конфликты и выбор стратегии).
		- Импорт из HTML: что будет при конфликте
			- Если импортируемый HTML содержит `memus-export` с `article.id`, и такая статья уже есть, появится диалог:
				- “Оставить существующую” — пропустить импорт этой страницы
				- “Перезаписать” — заменить содержимое существующей статьи
				- “Создать копию” — создать новую статью (обычно с префиксом вида `ver_YYYYMMDD_HHMMSS`)
				- (при пакетном импорте) можно включить “применить ко всем конфликтам”
		- Импорт из резервной копии (папка): меню списка → “Импорт из резервной копии (папка)”.
		- Импорт из Markdown/Logseq: меню списка → соответствующий пункт (может спросить `assetsBaseUrl`).
	- Проверка
		- У вас есть “план эвакуации”: HTML/ZIP можно хранить отдельно.
	- Если не получилось
		- Если ZIP пустой — проверьте, что у вас есть хотя бы одна не удалённая статья.

- # Шаг 15. Публичность, шифрование и Telegram (опциональные финишные возможности)
	- Публичная ссылка (делиться только чтением)
		- В меню статьи (“…”) нажмите “Дать доступ по ссылке”.
		- Нажмите кнопку 🌐 рядом с заголовком и откройте публичную страницу.
		- Убедитесь, что там нельзя редактировать.
	- Шифрование статьи (скрыть приватное)
		- Откройте обычную статью (не Inbox).
		- “… → Зашифровать” → задайте пароль и подсказку.
		- Закройте статью и откройте снова: должен попросить пароль.
		- Опционально: “… → Сменить пароль” и “… → Снять защиту”.
	- Telegram → Inbox
		- Меню пользователя → “Привязать Telegram” → скопируйте команду /link ... и отправьте боту.
		- Отправьте боту сообщение и проверьте, что оно появилось в Inbox.
	- Проверка
		- Вы умеете: делиться результатом, защищать приватное, собирать входящий поток без открытия Memus.
	- Если не получилось
		- Inbox нельзя шифровать и нельзя удалять как статью — это специальный раздел.

- # Песочница (расширенная): тренируем всё на одном месте
	- Правила песочницы
		- Здесь можно “ломать” и откатывать: Ctrl+Z и корзина блоков вас спасут.
		- Если что-то пошло совсем не так — удалите блоки и начните заново.

	- Тренажёр 1: навигация, выделение, сворачивание
		- Задание
			- Выделяй блоки стрелками ↑/↓.
			- Расширяй выделение Shift+↑/↓ (выдели 3 блока подряд).
			- Сверни/разверни ветку кнопкой слева и стрелками ←/→.
		- Заготовки (не удаляй, работай с ними)
			- Навигация: цель — быстро найти нужный блок
			- Выделение: выдели меня + ещё 2 блока
			- Сворачивание: я стану “родителем”
				- Ребёнок 1
				- Ребёнок 2

	- Тренажёр 2: вложенность и порядок без мыши
		- Задание
			- Сделай B дочерним у A (выдели B → Ctrl+→).
			- Верни B обратно на уровень (Ctrl+←).
			- Перемести C выше/ниже в своей ветке (Ctrl+Shift+↑/↓).
		- Заготовки
			- A
			- B
			- C

	- Тренажёр 3: Drag Mode (DnD блоков)
		- Подготовка
			- Убедись, что ты в режиме просмотра (не редактируешь текст).
			- Включи кнопку “Двигать” в тулбаре статьи.
		- Задание
			- Перетащи 3 ВНУТРЬ 1 (дроп в середину 1).
			- Перетащи 2 ДО 1 (дроп в верх 1).
			- Попробуй перетащить 1 внутрь своего потомка — убедись, что так нельзя.
			- Откати 2 шага Ctrl+Z, затем верни Ctrl+Y.
		- Заготовки
			- 1
				- 1.1
				- 1.2
			- 2
			- 3

	- Тренажёр 4: редактирование, разделение, слияние
		- Задание
			- Открой редактирование блока Enter и допиши текст.
			- Нажми Ctrl+↓ чтобы разделить блок по курсору.
			- Выдели 2–3 блока и нажми “Слияние” в тулбаре.
		- Заготовки
			- Длинный блок: поставь курсор в середину и раздели
			- Блок для слияния 1
			- Блок для слияния 2

	- Тренажёр 5: таблицы (вставка и управление)
		- Задание
			- Включи редактирование любого блока (Enter).
			- Нажми “Таблица” в тулбаре → заполни 2×2.
			- Кликни по ячейке и через панель таблицы добавь/удали строку и колонку.
		- Заготовка
			- Здесь будет таблица (вставь её в этот блок)

	- Тренажёр 6: форматирование через контекстное меню
		- Задание
			- В редактировании выдели текст → открой меню (ПКМ/долгий тап).
			- Применяй: жирный/курсив/список/код, затем “очистка”.
		- Заготовка
			- Форматирование: выдели слова и примени стиль

	- Тренажёр 7: ссылки (внешние и [[...]])
		- Задание
			- Вставь внешнюю ссылку на любой сайт.
			- Напиши [[Термины]] и сохрани блок.
			- Если ссылки не появилось — создай статью “Термины” и повтори сохранение.
		- Заготовка
			- Ссылки: [[Термины]] и https://example.com

	- Тренажёр 8: вставка “деревянного Markdown” как блоков из буфера
		- Задание
			- Перейди в режим просмотра (не редактирование).
			- Скопируй 4 строки ниже целиком и вставь Ctrl+V — должны появиться новые блоки.
		- Текст для копирования (вставляй как есть)
			- - Заголовок вставки
			- 	- Дочерний 1
			- 	- Дочерний 2
			- - Ещё один корневой

	- Тренажёр 9: вложения
		- Задание
			- Открой редактирование блока Enter.
			- Нажми кнопку “Файл” (внизу блока) и прикрепи 1 картинку и/или документ.
			- Сохрани блок и проверь, что вложение открывается.
		- Заготовка
			- Вложения: прикрепи файл сюда

	- Тренажёр 10: корзина блоков
		- Задание
			- Удали один из блоков кнопкой “Удалить” в тулбаре.
			- Открой “Корзина” в тулбаре → восстанови удалённый блок.
			- Затем снова открой “Корзина” → выбери “Очистить корзину” (если хочешь потренировать).
		- Заготовка
			- Удаляй меня в рамках тренировки

	- Тренажёр 11: экспорт
		- Задание
			- Выдели любой блок и нажми “Экспорт” (экспорт текущего блока в HTML).
			- В меню статьи (“…”) сделай “Экспорт в HTML” всей статьи.
		- Проверка
			- У тебя скачались 2 файла: фрагмент и статья

	- # Приложение: шпаргалка горячих клавиш
		- Режим просмотра (view)
			- Навигация: ↑/↓ — выбрать соседний блок; Enter — начать редактирование
			- Мультивыделение: Shift+↑/↓ — расширить/сузить диапазон выделенных блоков
			- Структура: Ctrl+Shift+↑/↓ — переместить блок (с детьми); Ctrl+→/← — вложить/поднять уровень
			- Удаление: Ctrl+Delete — удалить текущий блок (если не последний)
			- Сворачивание: Space — свернуть/развернуть выбранный блок
	- Режим редактирования (edit)
		- Ctrl+Enter — сохранить и выйти в просмотр
		- Ctrl+↓ — разрезать блок по курсору
		- Ctrl+→/← — вложить/поднять блок, оставаясь в редактировании
		- Esc — отменить редактирование (пустой блок удаляется)
	- Общие
		- Ctrl+Z / Ctrl+Y — отмена/повтор
		- В модальных окнах: Enter — подтвердить; Esc — закрыть
